import React from 'react';

const AddButton = ({ title, description, imageName, price }) => {
    return (
        <button className="add-button">Add</button>            
    );
};

export default AddButton;